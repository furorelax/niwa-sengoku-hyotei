# 丹羽戦国評定 開発ルール

## 正本

- 現行のプレイ可能なスキルは `skills/niwa-sengoku-hyotei/` に置く。
- スキル内の既存ファイルは、明示的な変更依頼がない限り内容を変更しない。
- キャラクター設定、史実、口調、物語、演出方針は原則としてMarkdownに保持する。
- 状態、合法手、数値計算、時間進行など、誤判定を避ける必要がある処理はTypeScriptへ段階的に移す。

## バージョン管理

- 正本MarkdownとJSONは固定ファイル名を維持し、版管理はGitコミット・タグ・Releaseで行う。
- 修正時に版番号付きの複製ファイルを作らず、固定名の正本を更新する。
- 丹羽ゲームのファイル更新報告では、指示ファイルを更新したかどうかを明記する。
- 既存版の削除、改名、内容の機械的な統合は、明示的な依頼なしに行わない。

## 開発領域

- 決定論的なゲームロジックは `src/core/` に置く。
- MCPサーバーとツール定義は `src/mcp/` に置く。
- 自動テストは `tests/` に置く。
- 設計資料は `docs/` に置く。
- プラグインのマニフェストと互換設定は `.codex-plugin/` に置く。

## 実装方針

- まず既存Markdownから自然文判断になっている状態・判定・進行管理を洗い出す。
- 「TypeScriptで厳格化するもの」「Markdownに残すもの」「要検討」に分類してから実装する。
- 現在の会話型・縦読み型ゲーム体験を維持し、GUI化や数値管理を目的化しない。
- 要求されていない機能や依存関係を追加しない。
- コード追加後は関連テストを実行し、結果と未検証事項を報告する。

## スキル

- `SKILL.md` の起動条件と参照先が収録ファイルに一致することを確認する。
- スキルを変更した場合は、スキル形式の検証を実行する。
- この開発リポジトリの変更だけで、ChatGPTにインストール済みの個人スキルが自動更新されるとは扱わない。
